import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { readSessionCookie } from "@/lib/auth";
import { saveOutcomeUpload } from "@/lib/save-image-upload";
type Params = { id: string }; const MAX_BYTES = 50 * 1024 * 1024;
const allowed: Record<string, { kind: "IMAGE" | "VIDEO" | "DOCUMENT"; ext: string }> = { "image/jpeg": { kind: "IMAGE", ext: ".jpg" }, "image/png": { kind: "IMAGE", ext: ".png" }, "image/webp": { kind: "IMAGE", ext: ".webp" }, "image/gif": { kind: "IMAGE", ext: ".gif" }, "video/mp4": { kind: "VIDEO", ext: ".mp4" }, "video/webm": { kind: "VIDEO", ext: ".webm" }, "video/quicktime": { kind: "VIDEO", ext: ".mov" }, "application/pdf": { kind: "DOCUMENT", ext: ".pdf" } };
export const runtime = "nodejs";
export async function POST(req: Request, ctx: { params: Promise<Params> }) { const session = await readSessionCookie(); if (!session) return NextResponse.json({ message: "未登录" }, { status: 401 }); const { id: taskId } = await ctx.params; const task = await prisma.task.findUnique({ where: { id: taskId }, select: { id: true, title: true, claims: { where: { userId: session.sub, status: "CLAIMED" }, select: { id: true } } } }); if (!task) return NextResponse.json({ message: "任务不存在" }, { status: 404 }); const canManage = session.role === "ADMIN" || session.role === "MINISTER"; if (!canManage && task.claims.length === 0) return NextResponse.json({ message: "仅任务参与者或管理人员可补充成果材料" }, { status: 403 }); const form = await req.formData().catch(() => null); const file = form?.get("file"); if (!(file instanceof File)) return NextResponse.json({ message: "缺少文件" }, { status: 400 }); const type = allowed[file.type]; if (!type) return NextResponse.json({ message: "仅支持 JPG、PNG、WebP、GIF、MP4、WebM、MOV 和 PDF" }, { status: 400 }); if (!file.size || file.size > MAX_BYTES) return NextResponse.json({ message: "单个材料不能超过 50 MB" }, { status: 400 }); const url = await saveOutcomeUpload(type.kind, Buffer.from(await file.arrayBuffer()), type.ext); const outcome = await prisma.taskOutcome.upsert({ where: { taskId }, create: { taskId, title: `${session.displayName}+${task.title}+其他`, updatedById: session.sub }, update: { updatedById: session.sub } }); const count = await prisma.taskOutcomeAsset.count({ where: { outcomeId: outcome.id } }); const asset = await prisma.taskOutcomeAsset.create({ data: { outcomeId: outcome.id, kind: type.kind, url, filename: file.name.slice(0, 191), mimeType: file.type, sizeBytes: file.size, sort: count, uploadedById: session.sub }, include: { uploadedBy: { select: { id: true, displayName: true } } } }); return NextResponse.json({ asset: { ...asset, createdAt: asset.createdAt.toISOString() } }); }
